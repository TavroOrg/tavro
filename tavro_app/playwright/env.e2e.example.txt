# Copy this file to .env.e2e and fill in your credentials.

# Your Zitadel login email / username
E2E_USERNAME=test_user

# Your Zitadel password
E2E_PASSWORD=test_password

# App base URL (change if running against staging instead of local)
E2E_BASE_URL=http://localhost:9000

# Backend API base URL (change if running against staging instead of local)
E2E_API_URL=http://localhost:8000

# Anthropic API key (used in Playground tests)
ANTHROPIC_API_KEY=sk-ant-...

# Azure AI Foundry credentials
AZURE_AI_FOUNDRY_ENDPOINT=https://your-resource.services.ai.azure.com/api/projects/your-project
AZURE_AI_FOUNDRY_KEY=your-azure-key-here
AZURE_AI_FOUNDRY_API_VERSION=2024-02-15-preview
AZURE_AI_FOUNDRY_AGENT_API_VERSION=v1

# Optional: Azure access token for creating agents. Generate with:
# az account get-access-token --resource https://ai.azure.com --query accessToken -o tsv
#AZURE_AI_FOUNDRY_AGENT_TOKEN=

# Optional. If omitted, the selected playground model is used as the Azure deployment name.
AZURE_AI_FOUNDRY_DEPLOYMENT=gpt-5.4-mini
